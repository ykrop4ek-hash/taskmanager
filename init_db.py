# init_db.py
from database import db
import sys
import os


def init_database():
    """Инициализирует базу данных"""
    print("🔄 Инициализация базы данных...")

    # Таблицы уже создаются автоматически при подключении
    # Но можно добавить дополнительную логику

    # Проверяем подключение
    try:
        db.cursor.execute("SELECT COUNT(*) FROM tasks")
        count = db.cursor.fetchone()['count']
        print(f"✅ База данных готова. Задач в базе: {count}")
    except Exception as e:
        print(f"❌ Ошибка при проверке базы данных: {e}")

    db.disconnect()


if __name__ == "__main__":
    init_database()